#include "main.h"
#include <stdio.h>
/**
 * add - function sum
 * @a: first number to sum
 * @b: second number to sum
 * Return: sum
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
