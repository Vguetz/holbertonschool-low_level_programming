#include <stdio.h>
/**
 * print_last_digit - function
 * @n: number
 *
 * Return: last digit of a number
 *
 */
int print_last_digit(int n)
{
	if (n < 10)
	{
		return (n);
	}
	n = n % 10;
	return (n);
}
