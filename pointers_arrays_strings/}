#include "main.h"
#include <stdio.h>

void print_array(int *a, int n)
{
	int size = n;
	int i = 0;

	while (i < size)
	{
		printf("%d", a[i]);
		if (i < n - 1)
		{
		printf(", ");
		}
		i++;
	}
	putchar('\n');
}
